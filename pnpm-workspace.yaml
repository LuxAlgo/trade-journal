packages:
  - "packages/*"
  - "apps/*"
allowBuilds:
  esbuild: true
  # v13 ships N-API prebuilds and sets gypfile:false. Approving the build makes
  # pnpm run node-gyp anyway, which fails on machines without a C++ toolchain.
  better-sqlite3: false
ignoredOptionalDependencies:
  - sharp
